<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
</head>
<body>

  <h1>{{ title }}</h1>

  <ul>
    {% for post in collections.posts %}
      <li>
        <h2>{{ post.data.title }}</h2>
        {{ post.content | safe }}
      </li>
    {% endfor %}
  </ul>

</body>
</html>
